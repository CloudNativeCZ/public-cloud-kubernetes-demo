apiVersion: v1
kind: Service
metadata:
  name: redis-master
  labels:
    app: backend
    role: master
    tier: backend
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: backend
    role: master
    tier: backend
