apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
    role: master
    tier: backend
spec:
  type: LoadBalancer
  loadBalancerIP: 137.117.242.53
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: backend
    role: master
    tier: backend
